https://live.api.qcloud.com/v2/index.php?Action=CreateLVBChannel&channel
 
 
 // -- 测试该内容
 
 	@Override
	protected void onStop() {
		super.onStop();
		if (mResVo.isVideo()){ // 属于视频才进行处理
			aaf_videoview.stopPlayback();
			aaf_videoview.release(true);
			aaf_videoview.stopBackgroundPlay();
			// --
			IjkMediaPlayer.native_profileEnd();
			// --
			isStart = false;
			// ===========
//			// 属于切换全屏			
//			if (afPlayerControl.isToggleFullScreen()){
//				onPlayPause();
//			} else {
//				aaf_videoview.stopPlayback();
//				aaf_videoview.release(true);
//				aaf_videoview.stopBackgroundPlay();
//				// --
//				isStart = false;
//			}
		}
	}
 
 Name=Live-888&outputSourceType=2&sourceList.1.name=Live-888&sourceList.1.type=1
